export default function Pill({ children }) {
  return (
    <span className="inline-block font-pixel text-[16px] border-2 border-copper px-3 py-2 text-cream bg-surface">
      {children}
    </span>
  )
}
